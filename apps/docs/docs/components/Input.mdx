---
sidebar_position: 1
---

# Input

## Preview

import Input from "@repo/ui/Input";
import Container from "./Container";
import { BadgeCheck } from "lucide-react";
import RedirectLink from "../../src/components/RedirectLink";

<Container>
  <Input />
</Container>

```tsx
import Input from "@repo/ui/Input";

<Input />;
```

## Installation

```bash
npx the-dig@latest add Input
```

## Props

| Prop             | Type                                    | Default                                                                                                        |
| ---------------- | --------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `variant`        | `"primary"` \| `"secondary"`            | `"primary"`                                                                                                    |
| `hasError`       | `boolean`                               | `false`                                                                                                        |
| `startAdornment` | `ReactNode`                             | -                                                                                                              |
| `endAdornment`   | `ReactNode`                             | -                                                                                                              |
| Others           | `InputHTMLAttributes<HTMLInputElement>` | <RedirectLink href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement">Document</RedirectLink> |

## Variants

<Container>
  <Input variant="primary" />
  <Input variant="secondary" />
</Container>

```tsx
import Input from "@repo/ui/Input";

<Input variant="primary" />
<Input variant="secondary" />
```

## Has Error

<Container>
  <Input hasError />
</Container>

```tsx
import Input from "@repo/ui/Input";

<Input hasError />;
```

## Start Adornment

<Container>
  <Input startAdornment={<BadgeCheck size={18} />} />
</Container>

```tsx
import Input from "@repo/ui/Input";
import { BadgeCheck } from "lucide-react";

<Input startAdornment={<BadgeCheck size={18} />} />;
```

## End Adornment

<Container>
  <Input endAdornment={<BadgeCheck size={18} />} />
</Container>

```tsx
import Input from "@repo/ui/Input";
import { BadgeCheck } from "lucide-react";

<Input endAdornment={<BadgeCheck size={18} />} />;
```
