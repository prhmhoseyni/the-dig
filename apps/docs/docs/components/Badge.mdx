---
sidebar_position: 1
---

# Badge

## Preview

import Badge from "@repo/ui/Badge";
import Container from "./Container";
import RedirectLink from "../../src/components/RedirectLink";

<Container>
  <Badge>به من توجه کن</Badge>
</Container>

```tsx
import Badge from "@repo/ui/Badge";

<Badge variant="contained" color="brand">
  به من توجه کن
</Badge>;
```

## Installation

```bash
npx the-dig@latest add Badge
```

## Props

| Prop      | Type                                                                          | Default                                                                                                   |
| --------- | ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `variant` | `"contained"` \| `"tinted"`                                                   | `"contained"`                                                                                             |
| `color`   | `"brand"` \| `"info"` \| `"success"` \| `"warning"` \| `"danger"` \| `"gray"` | `"brand"`                                                                                                 |
| Others    | `HTMLAttributes<HTMLDivElement>` (e.g. `onClick`, `disabled`, etc.)           | <RedirectLink href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement">Document</RedirectLink> |

## Variants and Colors

### Brand

<Container>
  <Badge variant="contained" color="brand">
    به من توجه کن
  </Badge>
  <Badge variant="tinted" color="brand">
    به من توجه کن
  </Badge>
</Container>

```tsx
import Badge from "@repo/ui/Badge";

<Badge variant="contained" color="brand">
  به من توجه کن
</Badge>
<Badge variant="tinted" color="brand">
  به من توجه کن
</Badge>
```

### Info

<Container>
  <Badge variant="contained" color="info">
    به من توجه کن
  </Badge>
  <Badge variant="tinted" color="info">
    به من توجه کن
  </Badge>
</Container>

```tsx
import Badge from "@repo/ui/Badge";

<Badge variant="contained" color="info">
  به من توجه کن
</Badge>
<Badge variant="tinted" color="info">
  به من توجه کن
</Badge>
```

### Success

<Container>
  <Badge variant="contained" color="success">
    به من توجه کن
  </Badge>
  <Badge variant="tinted" color="success">
    به من توجه کن
  </Badge>
</Container>

```tsx
import Badge from "@repo/ui/Badge";

<Badge variant="contained" color="success">
  به من توجه کن
</Badge>
<Badge variant="tinted" color="success">
  به من توجه کن
</Badge>
```

### Warning

<Container>
  <Badge variant="contained" color="warning">
    به من توجه کن
  </Badge>
  <Badge variant="tinted" color="warning">
    به من توجه کن
  </Badge>
</Container>

```tsx
import Badge from "@repo/ui/Badge";

<Badge variant="contained" color="warning">
  به من توجه کن
</Badge>
<Badge variant="tinted" color="warning">
  به من توجه کن
</Badge>
```

### Danger

<Container>
  <Badge variant="contained" color="danger">
    به من توجه کن
  </Badge>
  <Badge variant="tinted" color="danger">
    به من توجه کن
  </Badge>
</Container>

```tsx
import Badge from "@repo/ui/Badge";

<Badge variant="contained" color="danger">
  به من توجه کن
</Badge>
<Badge variant="tinted" color="danger">
  به من توجه کن
</Badge>
```

### Gray

<Container>
  <Badge variant="contained" color="gray">
    به من توجه کن
  </Badge>
  <Badge variant="tinted" color="gray">
    به من توجه کن
  </Badge>
</Container>

```tsx
import Badge from "@repo/ui/Badge";

<Badge variant="contained" color="gray">
  به من توجه کن
</Badge>
<Badge variant="tinted" color="gray">
  به من توجه کن
</Badge>
```
