---
sidebar_position: 1
---

# Toast

## Sonner

import RedirectLink from "../../src/components/RedirectLink";

<RedirectLink href="https://sonner.emilkowal.ski">Document</RedirectLink>

### Installation

```bash
npm install sonner
```

### Setup

```tsx
import { Toaster } from "sonner";

<Toaster />;
```

### Usage

```tsx
import { toast } from "sonner";

toast("Event has been created.");
```

## Preview

import Toast from "@repo/ui/Toast";
import Button from "@repo/ui/Button";
import { Toaster, toast } from "sonner";
import Container from "./Container";

<Container>
  <Toaster closeButton />

  <button
    onClick={() =>
      toast(
        <Toast
          title="به‌روزرسانی جدید"
          description="نسخه جدید آماده نصب است. برای بهبود عملکرد به‌روزرسانی کنید."
          actions={
            <div className="flex gap-4">
              <Button color="gray" variant="outlined">
                مشاهده تغییرات
              </Button>

              <Button color="gray" variant="outlined">
                به‌روزرسانی
              </Button>
            </div>
          }
        />,
      )
    }

>

    نمایش

  </button>
</Container>

```tsx
import Toast from "@repo/ui/Toast";
import { Toaster, toast } from "sonner";

<Toaster closeButton />

<button
  onClick={() =>
    toast(
      <Toast
        title="به‌روزرسانی جدید"
        description="نسخه جدید آماده نصب است. برای بهبود عملکرد به‌روزرسانی کنید."
        actions={
          <div className="flex gap-4">
            <Button color="gray" variant="outlined">
              مشاهده تغییرات
            </Button>

            <Button color="gray" variant="outlined">
              به‌روزرسانی
            </Button>
          </div>
        }
      />,
    )
  }
>
  نمایش
</button>
```

## Installation

```bash
npx dig-ui@latest add Toast
```

## Props

| Prop                                                               | Type           | Default |
| ------------------------------------------------------------------ | -------------- | ------- |
| `title` <sup className="text-red-600 text-caption2">\*</sup>       | `ReactNode`    | -       |
| `description` <sup className="text-red-600 text-caption2">\*</sup> | `ReactNode`    | -       |
| `actions` <sup className="text-red-600 text-caption2">\*</sup>     | `ReactNode`    | -       |
| `className`                                                        | `string`       | -       |
| `onClose`                                                          | `VoidFunction` | -       |
