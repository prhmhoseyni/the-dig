---
sidebar_position: 1
---

# CircularProgress

## Usage

import CircularProgress from "@repo/ui/CircularProgress";
import Container from "./Container";

<Container>
  <CircularProgress />
</Container>

```tsx
import CircularProgress from "@repo/ui/CircularProgress";

<CircularProgress />;
```

## Installation

```bash
npx the-dig@latest add CircularProgress
```

## Props

Add a visible or screen-reader loading label and appropriate progress semantics when the indicator represents an active status rather than decoration.

| Prop    | Type                                                                          | Default   |
| ------- | ----------------------------------------------------------------------------- | --------- |
| `color` | `"brand"` \| `"info"` \| `"success"` \| `"warning"` \| `"danger"` \| `"gray"` | `"brand"` |
| `size`  | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"`                                           | `"md"`    |
| `className`                                                | `string`                             | -           |

## Colors

<Container>
  <CircularProgress color="brand" />
  <CircularProgress color="info" />
  <CircularProgress color="success" />
  <CircularProgress color="warning" />
  <CircularProgress color="danger" />
  <CircularProgress color="gray" />
</Container>

```tsx
import CircularProgress from "@repo/ui/CircularProgress";

<CircularProgress color="brand" />
<CircularProgress color="info" />
<CircularProgress color="success" />
<CircularProgress color="warning" />
<CircularProgress color="danger" />
<CircularProgress color="gray" />
```

## Sizes

<Container>
  <CircularProgress size="xs" />
  <CircularProgress size="sm" />
  <CircularProgress size="md" />
  <CircularProgress size="lg" />
  <CircularProgress size="xl" />
</Container>

```tsx
import CircularProgress from "@repo/ui/CircularProgress";

<CircularProgress size="xs" />
<CircularProgress size="sm" />
<CircularProgress size="md" />
<CircularProgress size="lg" />
<CircularProgress size="xl" />
```
