---
sidebar_position: 1
---

# Textarea

## Preview

import Textarea from "@repo/ui/Textarea";
import { BadgeCheck } from "lucide-react";
import Container from "./Container";
import RedirectLink from "../../src/components/RedirectLink";

<Container>
  <Textarea />
</Container>

```tsx
import Textarea from "@repo/ui/Textarea";

<Textarea />;
```

## Installation

```bash
npx the-dig@latest add Textarea
```

## Props

| Prop                | Type                                          | Default                                                                                                           |
| ------------------- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `variant`           | `"primary"` \| `"secondary"`                  | `"primary"`                                                                                                       |
| `hasError`          | `boolean`                                     | `false`                                                                                                           |
| `rows`          | `number`                                     | `2` 
| `startAdornment`    | `ReactNode`                                   | -                                                                                                                 |
| `inputWrapperProps` | `HTMLAttributes<HTMLDivElement>`              | <RedirectLink href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement">Document</RedirectLink>         |
| Others              | `TextareaHTMLAttributes<HTMLTextAreaElement>` | <RedirectLink href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLTextAreaElement">Document</RedirectLink> |

## Variants

<Container>
  <Textarea variant="primary" />
  <Textarea variant="secondary" />
</Container>

```tsx
import Textarea from "@repo/ui/Textarea";

<Textarea variant="primary" />
<Textarea variant="secondary" />
```

## Has Error

<Container>
  <Textarea hasError />
</Container>

```tsx
import Textarea from "@repo/ui/Textarea";

<Textarea hasError />;
```

## Start Adornment

<Container>
  <Textarea startAdornment={<BadgeCheck size={18} />} />
</Container>

```tsx
import Textarea from "@repo/ui/Textarea";
import { BadgeCheck } from "lucide-react";

<Textarea startAdornment={<BadgeCheck size={18} />} />;
```
