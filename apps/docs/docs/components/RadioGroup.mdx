---
sidebar_position: 1
---

# RadioGroup

## Usage

import RadioGroup from "@repo/ui/RadioGroup";
import Container from "./Container";
import RedirectLink from "../../src/components/RedirectLink";

<Container>
  <RadioGroup name="radio" value="1" />
  <RadioGroup name="radio" value="2" />
  <RadioGroup name="radio" value="3" />
</Container>

```tsx
import RadioGroup from "@repo/ui/RadioGroup";

<RadioGroup name="radio" value="1" />
<RadioGroup name="radio" value="2" />
<RadioGroup name="radio" value="3" />
```

## Installation

```bash
npx the-dig@latest add RadioGroup
```

## Props

This export is a styled single radio input, not a group controller. Group radios with a shared `name` and use a labelled `fieldset`/`legend`.

| Prop   | Type                                                                        | Default                                                                                                        |
| ------ | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `className`                                                | `string`                             | -           |
| Others | `InputHTMLAttributes<HTMLInputElement>` (e.g. `onChange`, `disabled`, etc.) | <RedirectLink href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement">Document</RedirectLink> |

## Disabled

<Container>
  <RadioGroup defaultChecked disabled />
</Container>

```tsx
import RadioGroup from "@repo/ui/RadioGroup";

<RadioGroup defaultChecked disabled />;
```
