---
sidebar_position: 1
---

# Checkbox

## Preview

import Checkbox from "@repo/ui/Checkbox";
import Container from "./Container";
import RedirectLink from "../../src/components/RedirectLink";

<Container>
  <Checkbox />
</Container>

```tsx
import Checkbox from "@repo/ui/Checkbox";

<Checkbox />;
```

## Installation

```bash
npx the-dig@latest add Checkbox
```

## Props

| Prop   | Type                                                                        | Default                                                                                                        |
| ------ | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Others | `InputHTMLAttributes<HTMLInputElement>` (e.g. `onChange`, `disabled`, etc.) | <RedirectLink href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement">Document</RedirectLink> |

## Disabled

<Container>
  <Checkbox defaultChecked disabled />
</Container>

```tsx
import Checkbox from "@repo/ui/Checkbox";

<Checkbox defaultChecked disabled />;
```

## Indeterminate

<Container>
  <Checkbox defaultChecked data-indeterminate />
</Container>

```tsx
import Checkbox from "@repo/ui/Checkbox";

<Checkbox defaultChecked data-indeterminate />;
```
